---
# vars file for new_os
groupsss: ['admin', 'backup', 'devops', 'admin']

user_list:
  backup:
    name: 'backup'
    shell: /bin/bash
    groups:
      - 'backup'
  adm-rkendzia:
    name: adm-rkendzia
    groups :
      - 'admin'
    shell: /bin/bash